<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ACOMv2.Views.CanvasPanelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ACOMv2.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:developerTools="using:CommunityToolkit.WinUI.DeveloperTools"
    mc:Ignorable="d" xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,0">
        <Grid.DataContext>
            ACOMv2.Models
        </Grid.DataContext>
        <!-- The same large TextBlock, wrapped in a ScrollViewer. -->
        <ScrollViewer MinHeight="200" MinWidth="200" 
                  HorizontalAlignment="Stretch" 
                  VerticalAlignment="Stretch"
              HorizontalScrollBarVisibility="Auto" 
              VerticalScrollBarVisibility="Auto">
            <Grid >

                <!--<developerTools:AlignmentGrid HorizontalStep="20"
                              Opacity="0.2"
                              VerticalStep="20" />-->

                
                <labs:CanvasView MinHeight="400"
                     VerticalAlignment="Stretch" Background="Red">
                    <Border x:ConnectionId='2' VerticalAlignment="Stretch" x:Name="Test_Border" MinWidth="64" MinHeight="64"  Margin="0,0,0,0" Background="Black" ManipulationMode="TranslateX,TranslateY"                                             >
                        <TextBlock Text="Hello, World!" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <Border x:ConnectionId='3' x:Name="Test_Border1" Width="64" Height="64"  Margin="0,0,0,0" Background="Black" ManipulationMode="TranslateX,TranslateY"                                             >
                        <TextBlock Text="Hello, World!" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </labs:CanvasView>


            </Grid>
        </ScrollViewer>
    </Grid>
</Page>

